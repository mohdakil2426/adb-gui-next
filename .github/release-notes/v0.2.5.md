# ADB GUI Next v0.2.5

## Downloads

**Not sure? Download Windows (64-bit) Installer.**

### Recommended

| For | File |
| --- | --- |
| **Windows (64-bit) — Installer** | `ADB-GUI-Next-v0.2.5-windows-64bit-setup.exe` |
| **Linux (64-bit) — AppImage** | `ADB-GUI-Next-v0.2.5-linux-64bit.AppImage` |
| **Linux (64-bit) — DEB** (Debian/Ubuntu) | `ADB-GUI-Next-v0.2.5-linux-64bit.deb` |
| **Linux (64-bit) — RPM** (Fedora/RHEL) | `ADB-GUI-Next-v0.2.5-linux-64bit.rpm` |

### Also available

| For | Files |
| --- | --- |
| Windows (64-bit) MSI / Portable | `…-windows-64bit.msi`, `…-windows-64bit-portable.zip` |
| Windows (32-bit) | `…-windows-32bit-setup.exe` / `.msi` / `-portable.zip` |
| Windows (ARM) | `…-windows-arm-setup.exe`, `…-windows-arm-portable.zip` |
| Linux (ARM) | `…-linux-arm.AppImage` / `.deb` / `.rpm` |

Use `SHA256SUMS.txt` to verify downloads when provided.

## Platform support

| Platform | Status |
| --- | --- |
| Windows (64-bit) | First-class |
| Windows (32-bit) | Shipped |
| Windows (ARM) | Shipped (setup; bundled tools need x86 emulation) |
| Linux (64-bit) | First-class (AppImage, DEB, RPM) |
| Linux (ARM) | Shipped (system PATH adb/fastboot) |
| macOS | **Builds paused** (code may exist; not a default release target) |

## Notes

- **Not code-signed.** Windows SmartScreen may warn; that is expected.
- App id: `com.astrixforge.adbguinext`. Display name: **ADB GUI Next**.
- Portable zip: extract to a **local** folder (avoid OneDrive cloud-only files), then run `ADB GUI Next.exe`.
- Bundled Windows `adb.exe` is often **32-bit** (Google platform-tools); that is normal on 64-bit Windows.
- Installers embed the WebView2 bootstrapper; portable builds still need WebView2 Runtime installed.
- ADB and fastboot are bundled on Windows (64-bit / 32-bit) and Linux (64-bit). Linux (ARM) uses host tools.
- Some advanced workflows need a real device, fastboot/recovery mode, or a host Android SDK for emulators.
