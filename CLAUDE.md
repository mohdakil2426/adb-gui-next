@AGENTS.md

Follow root `AGENTS.md` as the router. Then open the closest module guide (`src/AGENTS.md` for frontend, `src-tauri/AGENTS.md` for Rust) and only the docs listed in the routing table (`docs/project_rules.md`, `docs/architecture.md`, …).
