# Scrcpy and UI Overhaul Implementation Plan

> **For agentic workers:** Execute task-by-task in this worktree. **Do not commit.** Steps use checkbox syntax.

**Goal:** Ship official-binary scrcpy management, true-black Neutral theme, Rust app-icon cache, file open-in-editor, dashboard/marketplace/utilities polish, and docs — all on `feat/scrcpy-and-ui-overhaul`.

**Architecture:** Thin Tauri commands; fat domains (`scrcpy/`, `app_icons/`, marketplace GitHub). Frontend is display + DTO forms. IPC only via `src/desktop/`.

**Tech Stack:** Tauri 2, Rust 2024, React 19, Tailwind v4, shadcn Neutral, Vitest, cargo test.

**Spec:** `docs/internal/reports/closed/2026-08-16/2026-08-16-scrcpy-and-ui-overhaul-research.md`

## Global Constraints

- No commits, no push, no merge to main.
- No `sm:`/`md:` viewport breakpoints; container queries only.
- No charting libraries; no scrcpy source; no raw hex in components.
- Windows/Linux x64 first-class; macOS paths for future.
- Commands must be listed in `permissions/autogenerated.toml` (all three permission tables).
- Skip commit steps.

---

### Task 1: Neutral true-black theme + docs tokens

**Files:** `src/styles/global.css`, `src/AGENTS.md`, `docs/architecture.md` §12.2

- [x] Align light/dark tokens with research table (true black canvas in dark).

---

### Task 2: Scrcpy Rust domain + IPC + tests

**Files:** create `src-tauri/src/scrcpy/{mod,assets,install,launch,flags}.rs`, `commands/scrcpy.rs`; modify `lib.rs`, `Cargo.toml` (`tar`), permissions, `desktop/backend.ts`, `desktop/models.ts`.

**Produces:** `ScrcpyLaunchOptions` camelCase DTO; `build_args(&options, serial) -> Vec<String>`; SHA-256 verify; platform asset name helper.

Tests: asset name mapping, argv builder, sha256 match, refuse unknown flags.

---

### Task 3: Scrcpy view

**Files:** `src/features/scrcpy/*`, `viewConfig.tsx`, `navigation.ts`.

Tools section. First-run download CTA, then options + Launch + Update.

---

### Task 4: App icons Rust + FE virtualizer wiring

**Files:** `src-tauri/src/app_icons/`, command `get_app_icons`, `InstalledPackageList.tsx`.

Tests: zip raster pick, batch cap, cache key from apk path.

---

### Task 5: File explorer editor + hidden-file test

**Files:** `commands/files.rs`, path actions, row menu.

---

### Task 6: Dashboard identity fields + motion

**Files:** `adb/telemetry.rs`, `models.ts`, `IdentityPanel.tsx`.

---

### Task 7: Marketplace GitHub all releases + README

**Files:** `marketplace/github.rs`, `types.rs`, `AppDetailView`, markdown renderer.

---

### Task 8: Utilities reimagine + logcat snapshot

**Files:** utilities UI + `run_adb` helper for `logcat -d -t 200`.

---

### Task 9: CI checkout persist-credentials

**Files:** `.github/workflows/ci.yml` (and publish.yml if checkout present).

---

### Task 10: README + AGENTS + architecture + memory-bank

---

### Test plan

- `bun run lint:web`
- targeted vitest: permissions, scrcpy, marketplace, file explorer, dashboard
- `bun run format:rust:check` / `lint:rust`
- `cargo test --manifest-path src-tauri/Cargo.toml` (or `--no-run` if Windows 0xc0000139)
